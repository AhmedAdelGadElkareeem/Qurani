<?xml version="1.0" encoding="utf-8"?>

<pages:PopupPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
                  xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                  xmlns:pages="clr-namespace:Mopups.Pages;assembly=Mopups"
                  x:Class="WytSky.Mobile.Maui.Skoola.Views.Quarni.Centers.AddCenter">
     <Grid Padding="15" VerticalOptions="End">
            <Frame
                x:Name="ComplexEntryLayout"
                Padding="15"
                BackgroundColor="White"
                CornerRadius="20"
                IsVisible="{Binding IsComplexEntryVisible, Mode=TwoWay}">
                <VerticalStackLayout>
                    <Entry
                        BackgroundColor="White"
                        Placeholder="اسم المسجد عربي"
                        Text="{Binding CenterName, Mode=TwoWay}"
                        TextColor="Black" />

                    <Entry
                        BackgroundColor="White"
                        Placeholder="اسم المسجد انجليزي"
                        Text="{Binding CenterNameEn, Mode=TwoWay}"
                        TextColor="Black" />
                    
                    <Label
                        FontAttributes="Bold"
                        FontSize="16"
                        Text="أدخل العنوان:" />
                    <Entry
                        x:Name="CityEntry"
                        BackgroundColor="White"
                        Placeholder="أسم الحـى"
                        Text="{Binding Address, Mode=TwoWay}"
                        TextColor="Black" />

                    <Button
                        Margin="0,10,0,0"
                        Padding="10"
                        BackgroundColor="Blue"
                        Command="{Binding AddCenterCommand}"
                        Text="➕ Add Complex"
                        TextColor="White" />
                </VerticalStackLayout>
            </Frame>
        </Grid>
</pages:PopupPage >