<?xml version="1.0" encoding="utf-8"?>

<pages:PopupPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
                 xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                 xmlns:pages="clr-namespace:Mopups.Pages;assembly=Mopups"
                 x:Class="WytSky.Mobile.Maui.Skoola.Views.Quarni.Complexes.AddComplex">
    <Frame HeightRequest="300" 
           Margin="20,10"
           BackgroundColor="White"
           CornerRadius="10">
    <VerticalStackLayout
        Padding="15"
        BackgroundColor="White"
        VerticalOptions="CenterAndExpand">
    
        <!--  Entry for Complex Name  -->
        <Entry
            x:Name="ComplexNameEntry"
            BackgroundColor="White"
            Placeholder="اسم المجمع"
            HorizontalTextAlignment="Center"
            Text="{Binding ComplexName, Mode=TwoWay}"
            TextColor="Black" />
    
        <!--  Button to Add Complex  -->
        <Button
            Margin="0,10,0,0"
            Padding="10"
            BackgroundColor="Blue"
            Command="{Binding AddComplexCommand}"
            FontSize="18"
            Text=" اضافة"
            TextColor="White" />
    </VerticalStackLayout>
        </Frame>
</pages:PopupPage>